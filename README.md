# docs

https://exiahuang.github.io/

```
git init
git branch -m master main
git add .
git commit -m "init"
git remote add origin git@github.com:<USERNAME>/<REPOSITORY>.git 
git push -u origin main

```